{
  "buildCommand": "if [ -d \"flutter\" ]; then echo \"Flutter cached\"; else git clone https://github.com/flutter/flutter.git -b stable; fi && ./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web"
}
